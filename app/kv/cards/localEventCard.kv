

<LocalEventCard>:

    MDBoxLayout:
        # radius: 25
        orientation: 'horizontal'
        md_bg_color: [.8,.9,.9,1]

        padding: [25,25,25,25]

        # Left Side
        MDBoxLayout:
            size_hint_x: 0.35
            size_hint_y: 1
            md_bg_color: app.theme_cls.primary_color
            orientation: 'vertical'
            # padding: [5,50,5,50]
            # spacing: 25

            # radius: 25

            # Calender
            MDBoxLayout:
                orientation: "vertical"
                # padding: [10, 50, 10, 50]

                pos_hint: {"center_x":.5, "center_y": .8}

                size_hint_x: .75
                size_hint_y: 1
                # id: tag_table

                # Headers
                MDLabel:
                    
                    text: f"{root.get_month(root.month)}"
                    # font_style: "H4"
                    halign: "center"
                    # text_align:'center'
                    # valign: "top"
                    # padding: [100, 0, 50, 0]
                    font_size: self.width/8
                    size_hint_y: None  # This allows you to set a fixed height
                    height: max(self.texture_size[1], 10)  # Adjust the minimum height here
                    # height: self.height/3
                    # width: self.width/2
                    theme_text_color: "Custom"
                    text_color: 1, 1, 1, 1  # White text color
                    text_size: self.size
                    md_bg_color: [1, 0, 0, 1]  # Use the primary color as the outline color
                    # size_hint_x: .25
                    # size_hint: (1, .3)
                    # height: self.texture_size[1]  # Adjust the size based on text size
                    
                    
                
                MDLabel:
                    text: f"{int(root.day)}"
                    halign: "center"
                    valign: "center"
                    # font_style: "H4"
                    font_size: self.width/7
                    size_hint_y: None  # This allows you to set a fixed height
                    height: max(self.texture_size[1], 110)  # Adjust the minimum height here
                    # size_hint_x: .25
                    # padding: [100, 0, 50, 0]
                    # size_hint: (1, 1)
                    text_size: self.size
                    md_bg_color: [1, 1, 1, 1]  # Use the primary color as the outline color
                

            # Time
            MDLabel:
                text: f"{root.time}"
                halign: "center"
                # font_style: "H4"
                font_size: self.width/10
                theme_text_color: "Custom"
                text_color: [1,1,1,1]
                # md_bg_color: [0.9,0.9,0.9,1]
                # size_hint_y: None
                size_hint_x: 1
                # size_hint_y: None  # This allows you to set a fixed height
                # height: max(self.texture_size[1], 50)  # Adjust the minimum height here
                # height: self.texture_size[1] + 5
                # pos_hint: {"center_x": .8, "center_y": .5} 

            MDBoxLayout:
                size_hint_x: .75
                pos_hint: {"center_x": .5, "center_y": .5} 

                # Location Type    
                MDLabel:
                    text: f"{root.location_type}"
                    halign: "center"
                    valign: "center"
                    size_hint: (.5, .4)
                    # font_style: "H4"
                    font_size: self.width/8
                    theme_text_color: "Custom"
                    text_color: app.theme_cls.primary_color
                    md_bg_color: [1,1,1,1]
                    # size_hint_y: None
                    # padding: [20, 0, 0, 0]
                    # size_hint_x: .5
                    text_size: self.size
                    # size_hint_y: None  # This allows you to set a fixed height
                    # height: max(self.texture_size[1], 50)  # Adjust the minimum height here
                    # pos_hint: {"center_x": .5, "center_y": .5} 
                    # radius: 25

            # Location
            MDLabel:
                text: f"{root.location}"
                halign: "center"
                # size_hint_y: .2
                # font_style: "H4"
                font_size: self.width/8
                theme_text_color: "Custom"
                text_color: [1,1,1,1]
                # size_hint_y: None  # This allows you to set a fixed height
                # height: max(self.texture_size[1], 10)  # Adjust the minimum height here
                # padding: [20, 0, 0, 0]
                # size_hint_y: None
                # size_hint_x: 1
                # height: self.texture_size[1] + 5
                # pos_hint: {"center_x": 0.5, "center_y": 1} 




        MDBoxLayout:
            orientation: "vertical"
            size_hint_x: 0.65
            size_hint_y: 1
            radius: 15

            md_bg_color: [.7,.8,.8,1]

            padding: [20, 20, 20, 20]

            # Title
            MDLabel:
                text: f"{root.title}"
                size_hint_y: None
                height: self.texture_size[1] + 5
                font_size: self.width/10
                underline: True
                theme_text_color: "Custom"
                text_color: app.theme_cls.primary_color

            # General Descriptions
            MDLabel:
                size_hint_y: 1
                text: f"asd;lfkjads l;fjasdl fasdl;kjf a;dslkjf ;laskdjf a;sldkjf ;alskdj ;alkdjf ;alsdkjf ;lasdkjf ;alskdfj ;alkjf ;alksjf ;alksjf a;lskdf j"
                theme_text_color: "Custom"
                # font_style: "H4"
                valign: 'top'
                font_size: self.width/15
                text_size: self.size
                text_color: app.theme_cls.primary_color
                pos_hint: {"center_x":0.5, "center_y": 0.8}
                padding: [15,20,15,10]

            
            # Learn More
            MDFillRoundFlatIconButton:
                text: "Learn more"
                text_size: self.size
                font_style: "Body1"
                size_hint: (.7, .1)
                text_size: self.size
                pos_hint: {"center_x":0.5, "center_y": 0.5}
                # Adjust the size as needed
                # theme_text_color: "Custom"
                text_color: [1,1,1,1]  # Set text color to the primary color
                
                # Redirect to another website
                on_release: root.redirection_website()