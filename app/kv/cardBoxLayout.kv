<MyBoxLayout>:
    orientation: "horizontal"

    # Left section with a small square
    MDBoxLayout:
        orientation: "vertical"
        size_hint_x: None
        width: dp(50)  # Set the width of the left section
        spacing: dp(10)

    # Right section
    MDBoxLayout:
        orientation: "vertical"
        spacing: dp(10)
